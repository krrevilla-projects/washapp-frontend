import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  rootContainer: {
    flex: 1,
    padding: 20,
  },
  input: {
    marginBottom: 20,
  },
  dataContainer: {
    flex: 1,
    marginTop: 20,
  },
});

export default styles;
